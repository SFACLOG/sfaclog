import { RoundButton } from 'sfac-design-kit';

const page = () => {
  return (
    <div className='ml-5'>
      <RoundButton size='sm'>버튼이요</RoundButton>
      <RoundButton theme='disable'>버튼이요</RoundButton>
    </div>
  );
};

export default page;
